# SerialShell Example

The SerialShell example files (SerialShell.ino, SerialShell.h, SerialShell.cpp) are generated during the library packaging phase. See the project root for the current SerialShell source in src/ and include/.
